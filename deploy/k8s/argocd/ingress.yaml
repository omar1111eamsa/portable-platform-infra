# Ingress ArgoCD — accès UI via domaine ngrok
# URL: https://example.ngrok-free.dev/argocd
# Prérequis: appliquer le patch ArgoCD (voir README.md)
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server
  namespace: argocd
  annotations:
    traefik.ingress.kubernetes.io/router.priority: "250"
spec:
  ingressClassName: traefik
  rules:
    - host: "example.ngrok-free.dev"
      http:
        paths:
          - path: /argocd
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
    - http:
        paths:
          - path: /argocd
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
