apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatbot-data-pvc
  namespace: myapp
  labels:
    app: chatbot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
