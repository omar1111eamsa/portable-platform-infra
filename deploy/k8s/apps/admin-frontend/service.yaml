apiVersion: v1
kind: Service
metadata:
  name: admin-frontend
  namespace: myapp
  labels:
    app.kubernetes.io/name: admin-frontend
    app.kubernetes.io/component: frontend
spec:
  selector:
    app.kubernetes.io/name: admin-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
