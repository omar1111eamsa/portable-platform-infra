apiVersion: v1
kind: Service
metadata:
  name: admin-frontend
  namespace: myapp
  labels:
    app: admin-frontend
spec:
  selector:
    app: admin-frontend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP

