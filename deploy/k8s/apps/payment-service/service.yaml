apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: myapp
  labels:
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: payment-service
  ports:
    - name: http
      port: 8083
      targetPort: http
  type: ClusterIP
