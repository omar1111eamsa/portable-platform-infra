apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: myapp
  labels:
    app: api-gateway
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      automountServiceAccountToken: false
      nodeSelector:
        kubernetes.io/hostname: frontend-vm
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: api-gateway
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: api-gateway
          image: ghcr.io/myapp/backend-api-gateway:35fca3f
          imagePullPolicy: Always
          ports:
            - containerPort: 8888
          env:
            - name: SPRING_APPLICATION_NAME
              value: api-gateway
            - name: SERVER_PORT
              value: "8888"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: SPRING_CLOUD_CONSUL_HOST
              value: consul
            - name: SPRING_CLOUD_CONSUL_PORT
              value: "8500"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_ENABLED
              value: "true"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_REGISTER
              value: "true"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_DEREGISTER
              value: "true"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_PREFER_IP_ADDRESS
              value: "true"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_QUERY_PASSING
              value: "true"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_IP_ADDRESS
              value: "$(POD_IP)"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_INSTANCE_ID
              value: "$(POD_NAME)"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HEALTH_CHECK_PATH
              value: /actuator/health
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HEALTH_CHECK_CRITICAL_TIMEOUT
              value: 1m
            # K8s DNS (direct WebClient, not Consul) — service name: user-management
            - name: USER_SERVICE_URI
              value: "http://user-management:8081"
            - name: SPRING_REDIS_HOST
              value: redis
            - name: SPRING_REDIS_PORT
              value: "6379"
            # JWT secret must match user-management
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth-credentials
                  key: JWT_SECRET
            - name: FRONTEND_ORIGIN
              value: "https://dev.example.com,http://dev.example.com"
            - name: SPRING_APPLICATION_JSON
              value: '{"spring":{"cloud":{"gateway":{"globalcors":{"add-to-simple-url-handler-mapping":true,"cors-configurations":{"[/**]":{"allowed-origins":["https://dev.example.com","http://dev.example.com"],"allowed-headers":["*"],"allowed-methods":["GET","POST","PUT","DELETE","OPTIONS"],"allow-credentials":true}}}}}}}'
          resources:
            requests:
              memory: "320Mi"
              cpu: "200m"
            limits:
              memory: "576Mi"
              cpu: "1000m"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8888
            # API gateway can take several minutes under node pressure.
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 60
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8888
            initialDelaySeconds: 240
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8888
            initialDelaySeconds: 120
            periodSeconds: 5
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
