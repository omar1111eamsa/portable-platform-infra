apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: myapp
  labels:
    app: api-gateway
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8888
      targetPort: 8888
      name: http
  type: ClusterIP
