apiVersion: v1
kind: Service
metadata:
  name: prompttocode
  namespace: myapp
  labels:
    app: prompttocode
spec:
  selector:
    app: prompttocode
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
