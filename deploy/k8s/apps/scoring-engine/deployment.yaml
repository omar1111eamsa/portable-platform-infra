apiVersion: apps/v1
kind: Deployment
metadata:
  name: scoring-engine
  namespace: myapp
  labels:
    app.kubernetes.io/name: scoring-engine
    app.kubernetes.io/component: pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: scoring-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: scoring-engine
        app.kubernetes.io/component: pipeline
    spec:
      nodeSelector:
        kubernetes.io/hostname: myapp-worker-02
      containers:
        - name: scoring-engine
          image: ghcr.io/myapp/metamodel-scoring-engine:latest
          env:
            - name: DB_HOST
              value: "postgres"
            - name: DB_NAME
              value: "prediction_db"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2"
              memory: "2Gi"
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "import sys; sys.exit(0)"
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
