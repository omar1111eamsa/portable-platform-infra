apiVersion: v1
kind: Service
metadata:
  name: user-management
  namespace: myapp
  labels:
    app.kubernetes.io/name: user-management
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: user-management
  ports:
    - name: http
      port: 8081
      targetPort: http
  type: ClusterIP
