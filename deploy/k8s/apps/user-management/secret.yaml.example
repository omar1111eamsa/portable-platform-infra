apiVersion: v1
kind: Secret
metadata:
  name: mail-credentials
  namespace: myapp
  labels:
    app: user-management
type: Opaque
stringData:
  SPRING_MAIL_USERNAME: "your-smtp-username@example.com"
  SPRING_MAIL_PASSWORD: "replace-with-app-password-or-smtp-password"
