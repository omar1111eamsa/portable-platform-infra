apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-management-avatar-pvc
  namespace: myapp
  labels:
    app: user-management
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
